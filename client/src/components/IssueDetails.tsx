import type { ReactElement } from 'react';
import { useState } from 'react';
import { useDeleteIssue, useIssue, useUpdateIssue } from '../features/issues';
import type { Issue, IssueStatus } from '../types/issue';
import { CommentSection } from './CommentSection';
import { IssueHistory } from './IssueHistory';

export const IssueDetails = ({ issue, onDeleted }: { issue: Issue; onDeleted: () => void }): ReactElement => { const details = useIssue(issue._id); const update = useUpdateIssue(issue._id); const remove = useDeleteIssue(issue._id); const [status, setStatus] = useState(issue.status); const save = (value: IssueStatus) => { setStatus(value); void update.mutateAsync({ status: value }); }; return <aside className="space-y-5 rounded-xl bg-white p-5 shadow-sm"><div className="flex items-start justify-between gap-3"><div><p className="text-xs font-bold uppercase tracking-widest text-cyan-700">{issue.type}</p><h2 className="mt-1 text-xl font-bold text-slate-900">{issue.title}</h2></div><button className="font-semibold text-red-600" onClick={() => void remove.mutateAsync().then(onDeleted)}>Delete</button></div><p className="text-sm text-slate-500">{issue.description || 'No description'}</p><label className="block text-sm font-semibold">Status<select className="input mt-2" value={status} onChange={(event) => save(event.target.value as IssueStatus)}><option>OPEN</option><option>IN_PROGRESS</option><option>RESOLVED</option><option>CLOSED</option><option>REOPENED</option></select></label><CommentSection entityType="ISSUE" entityId={issue._id} />{details.data && <IssueHistory history={details.data.history} />}</aside>; };