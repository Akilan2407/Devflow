import { useRef, useState, type ChangeEvent, type ReactElement } from 'react';
import { useUploadAttachment } from '../features/attachments';
import type { AttachmentEntityType } from '../types/attachment';

const allowed = new Set(['jpg', 'jpeg', 'png', 'gif', 'webp', 'pdf', 'doc', 'docx', 'txt', 'zip']);
export const FileUploader = ({ entityType, entityId, onUploaded }: { entityType: AttachmentEntityType; entityId: string; onUploaded?: (file: File) => void }): ReactElement => { const input = useRef<HTMLInputElement>(null); const upload = useUploadAttachment(entityType, entityId); const [message, setMessage] = useState(''); const [progress, setProgress] = useState(0); const choose = async (event: ChangeEvent<HTMLInputElement>) => { const file = event.target.files?.[0]; if (!file) return; const extension = file.name.toLowerCase().split('.').pop() ?? ''; if (!allowed.has(extension) || file.size > 10 * 1024 * 1024) { setMessage('Use an allowed file type up to 10 MB.'); return; } setMessage('Uploading...'); setProgress(0); try { await upload.mutateAsync({ file, onProgress: setProgress }); setProgress(100); setMessage('Upload complete.'); onUploaded?.(file); } catch { setMessage('Upload failed.'); } finally { if (input.current) input.current.value = ''; } }; return <div className="space-y-2"><input ref={input} className="hidden" type="file" accept=".jpg,.jpeg,.png,.gif,.webp,.pdf,.doc,.docx,.txt,.zip" onChange={(event) => void choose(event)} /><button className="button max-w-fit" type="button" disabled={upload.isPending} onClick={() => input.current?.click()}>Add file</button>{upload.isPending && <div className="h-1.5 overflow-hidden rounded-full bg-slate-200"><div className="h-full bg-cyan-600" style={{ width: `${progress}%` }} /></div>}{message && <p className={`text-xs ${message.includes('failed') || message.includes('Use') ? 'text-red-600' : 'text-slate-500'}`}>{message}</p>}</div>; };