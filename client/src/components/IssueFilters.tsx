import type { ReactElement } from 'react';
import type { IssueFilters as Filters } from '../features/issues';

export const IssueFilters = ({ filters, onChange }: { filters: Filters; onChange: (filters: Filters) => void }): ReactElement => <div className="flex flex-wrap gap-3"><input className="input max-w-xs" placeholder="Search issues" value={filters.search ?? ''} onChange={(event) => onChange({ ...filters, search: event.target.value })} /><select className="input max-w-xs" value={filters.status ?? ''} onChange={(event) => onChange({ ...filters, status: event.target.value })}><option value="">All statuses</option><option>OPEN</option><option>IN_PROGRESS</option><option>RESOLVED</option><option>CLOSED</option><option>REOPENED</option></select><select className="input max-w-xs" value={filters.priority ?? ''} onChange={(event) => onChange({ ...filters, priority: event.target.value })}><option value="">All priorities</option><option>LOW</option><option>MEDIUM</option><option>HIGH</option><option>CRITICAL</option></select><select className="input max-w-xs" value={filters.severity ?? ''} onChange={(event) => onChange({ ...filters, severity: event.target.value })}><option value="">All severities</option><option>LOW</option><option>MEDIUM</option><option>HIGH</option><option>CRITICAL</option></select><select className="input max-w-xs" value={filters.type ?? ''} onChange={(event) => onChange({ ...filters, type: event.target.value })}><option value="">All types</option><option>BUG</option><option>FEATURE</option><option>TASK</option><option>IMPROVEMENT</option></select><input className="input max-w-xs" placeholder="Label" value={filters.label ?? ''} onChange={(event) => onChange({ ...filters, label: event.target.value })} /><input className="input max-w-xs" placeholder="Sprint ID" value={filters.sprintId ?? ''} onChange={(event) => onChange({ ...filters, sprintId: event.target.value })} /></div>;