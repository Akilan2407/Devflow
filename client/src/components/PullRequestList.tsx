import type { ReactElement } from 'react';
import type { GithubPullRequest } from '../types/github';

export const PullRequestList = ({ pullRequests }: { pullRequests: GithubPullRequest[] }): ReactElement => <section><h3 className="mb-3 text-lg font-bold">Pull requests</h3><div className="grid gap-3 md:grid-cols-2">{pullRequests.length ? pullRequests.map((pullRequest) => <a className="rounded-xl border border-slate-200 bg-white p-4 hover:border-cyan-500" href={pullRequest.html_url} target="_blank" rel="noreferrer" key={pullRequest.id}><div className="flex justify-between gap-3"><span className="text-sm text-slate-500">#{pullRequest.number}</span><span className="text-xs font-semibold uppercase text-cyan-700">{pullRequest.state}</span></div><p className="mt-2 font-semibold">{pullRequest.title}</p><p className="mt-2 text-sm text-slate-500">{pullRequest.user?.login ?? 'Unknown author'}</p></a>) : <p className="rounded-xl border border-slate-200 bg-white p-4 text-sm text-slate-500">No pull requests.</p>}</div></section>;