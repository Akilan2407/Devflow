import type { ReactElement } from 'react';
import type { Issue } from '../types/issue';

export const IssueList = ({ issues, onSelect }: { issues: Issue[]; onSelect: (issue: Issue) => void }): ReactElement => issues.length ? <div className="space-y-3">{issues.map((issue) => <button key={issue._id} className="w-full rounded-xl border border-slate-200 bg-white p-4 text-left hover:border-cyan-500" onClick={() => onSelect(issue)}><div className="flex flex-wrap items-center justify-between gap-3"><h3 className="font-bold text-slate-900">{issue.title}</h3><span className="text-xs font-bold uppercase text-cyan-700">{issue.status.replace('_', ' ')}</span></div><p className="mt-2 text-xs font-semibold uppercase text-slate-400">{issue.type} · {issue.priority} priority · {issue.severity} severity</p><p className="mt-2 text-sm text-slate-500">{issue.description || 'No description'}</p><div className="mt-3 flex flex-wrap gap-2">{issue.labels.map((label) => <span key={label} className="rounded bg-slate-100 px-2 py-1 text-xs text-slate-600">{label}</span>)}</div></button>)}</div> : <p className="rounded-xl border border-dashed border-slate-300 p-8 text-center text-slate-500">No issues match these filters.</p>;